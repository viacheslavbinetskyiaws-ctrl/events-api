apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: events-api
  namespace: events-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: events-api
  minReplicas: 1
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
