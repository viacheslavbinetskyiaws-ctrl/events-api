# What's Next

Resumption notes — where things stand, and how to bring everything back up after a Docker restart.

## Current state

- **Milestones 0–4**: done (see `PLAN.md` for full detail).
- **Milestone 5 (Terraform + LocalStack)**: done.
  - Provider config done: `terraform/versions.tf`, `variables.tf`, `providers.tf` — `use_locastack` variable (note: named without the `l` — `use_locastack`, not `use_localstack`; consistent across files so it works, just a naming quirk) drives a `dynamic "endpoints"` block pointing s3/ec2/iam at LocalStack.
  - `modules/networking/` — done, verified against LocalStack directly (VPC, 2 subnets in different AZs, security group).
  - `modules/iam/` — done, verified against LocalStack directly (trust policy, role, logs policy, attachment). Output bug (role_arn pointed at the policy's ARN, not the role's) found and fixed.
  - `modules/s3/` — done: `aws_s3_bucket` (`events-api-tfstate`) + `aws_s3_bucket_versioning` (Enabled) + `aws_s3_bucket_public_access_block` (all 4 flags blocked). No DynamoDB table — locking is handled natively (see backend below).
  - Remote state bootstrap — done: `terraform/backend.tf` holds a `backend "s3"` block (`bucket = "events-api-tfstate"`, `key = "events-api/terraform.tfstate"`, `use_lockfile = true` for native S3 locking). Backend blocks can't reference variables, so unlike `providers.tf` this one is hardcoded to the LocalStack endpoint — swapping to real AWS later means editing this file directly, not flipping `use_locastack`. Migrated via `terraform init -migrate-state`; state now actually lives at `s3://events-api-tfstate/events-api/terraform.tfstate` inside LocalStack. Local `terraform.tfstate` is now just an empty leftover file (harmless, not the source of truth anymore). `use_lockfile` has been exercised via a few `plan`s with no lock-acquisition errors, but hasn't been stress-tested for real concurrent-apply behavior on this LocalStack version.
  - Teardown/verify pass — done: reversed the bootstrap (commented out the `backend "s3"` block in `terraform/backend.tf`, `terraform init -migrate-state` back to local), added `force_destroy = true` to the `aws_s3_bucket.state` resource in `modules/s3/main.tf` (needed since the bucket has versioning enabled — a non-empty/versioned bucket refuses to delete otherwise), then `terraform destroy`. Verified clean directly against LocalStack afterward, not just via `terraform state list`: no buckets, no IAM roles, only LocalStack's own default VPC left. `backend.tf` is currently commented out and `terraform/modules/s3/main.tf` currently has `force_destroy = true` left in — both intentional leftovers from this pass, harmless to leave as-is since there's no live infrastructure right now.
- **Milestone 6 (Helm)**: done.
  - **Part A** — consumed `oci://registry-1.docker.io/bitnamicharts/postgresql` (v18.8.4; Bitnami's classic `charts.bitnami.com` HTTP repo is being phased out in favor of OCI). Installed release `pg-demo` into its own `pg-demo` namespace with `helm/bitnami-postgres/values-override.yaml` (custom `auth.username`/`auth.database`, `primary.persistence.size`). Exercised `helm upgrade` (added `primary.resources`), `helm rollback` (back to revision 1, confirming the chart's `resourcesPreset: nano` default takes over when no explicit override exists), and `helm uninstall`. Confirmed the known StatefulSet gotcha hands-on: the PVC (`data-pg-demo-postgresql-0`) survived `helm uninstall` since `volumeClaimTemplates`-created PVCs aren't Helm-owned — deleted it and the namespace manually afterward. Nothing from Part A is left running.
  - **Part B** — authored `helm/events-api/`, a real chart for the app itself. `helm create events-api`, then deleted the YAGNI boilerplate (`ingress.yaml`, `httproute.yaml` — Gateway API, new default in Helm v4.2.3, `hpa.yaml`, `serviceaccount.yaml`, `tests/`). Templatized `deployment.yaml`/`service.yaml` from `k8s/base` into `values.yaml`-driven equivalents; added `configmap.yaml`/`secret.yaml` (not in the original plan wording, but necessary — the app can't run without its env config). Migration Job (`migration-job.yaml`) uses `helm.sh/hook: pre-install,pre-upgrade` — same ordering problem `k8s/README.md`'s manual staged `kubectl apply` sequence solved, now handled declaratively. Key subtlety: `configmap.yaml`/`secret.yaml` also had to become hooks (`hook-weight: "-5"`, vs the Job's `"0"`) since hooks run *before* a chart's regular resources — otherwise the Job would fire before its own config/secret existed. The Job also needs `hook-delete-policy: before-hook-creation` since Job specs are immutable (breaks a second `helm upgrade` otherwise).
  - Postgres for Part B's chart is **not** part of the chart itself — deliberately reused the raw `k8s/base/postgres-{deployment,service,secret}.yaml` manifests (namespace swapped via `sed` from `events-api` to `events-api-helm`) applied directly with `kubectl`, matching real-world practice where an app's database is usually owned/lifecycled separately from the app's own release (managed service, Operator, or platform team's chart — not a Helm subchart dependency of the app that consumes it).
  - Namespaces in play now: `events-api` (Milestone 4's raw manifests, untouched, still the reference to compare against), `events-api-helm` (Part B's Postgres + the `events-api` Helm release).
- **Milestone 7 (multi-tenancy)**: done, local dev only so far (k8s/AWS credential-split follow-up below still open).
  - `tenant_id` added to `events` via migration `26e3e99a90fe` (nullable → backfill → `NOT NULL`), composite index `(tenant_id, occurred_at)` on `EventORM`. Applied and verified against local dev Postgres.
  - `migrations/env.py` now has `include_object` filtering so autogenerate stops proposing drops for dbt-owned tables (`daily_event_counts`) not in Alembic's `target_metadata` — general fix, not specific to one migration.
  - RLS added via migration `d7a67740cfa5`: new non-owner role `events_app` (grants: `SELECT, INSERT` on `events`, `SELECT` on `daily_event_counts`) + `ALTER TABLE events ENABLE ROW LEVEL SECURITY` + a policy keyed on `current_setting('app.current_tenant', true)`. `events`/Alembic/dbt stay on the owner role (superuser via `POSTGRES_USER`, bypasses RLS unconditionally — needed for dbt's cross-tenant mart aggregation). `app/core/config.py`'s `database_url` default now points at `events_app`; `migrations/env.py`'s default was decoupled from it and hardcoded to the owner role instead (`ALEMBIC_DATABASE_URL` still the override escape hatch).
  - **Follow-up resolved** (originally flagged here as not-yet-done): `k8s/base/migration-job.yaml` now has its own `k8s/base/migration-secret.yaml` (`events-api-migration-secrets`, key `ALEMBIC_DATABASE_URL`, owner role, `postgres:5432` in-cluster hostname) wired in via a third `envFrom` entry, kept as its own Secret rather than a new key on `events-api-secrets` — the owner-role credential (bypasses RLS, has DDL rights) must never land in the app Deployment's pod env, even unused, or it undermines the whole point of the RLS split (least privilege). `events-api-secrets`' `APP_DATABASE_URL` also flipped to `events_app`, so the app now actually runs RLS-enforced in k8s too, not just in local dev. Same two-credential split will apply later for the real-AWS phase (Secrets Manager/IRSA instead of a k8s Secret, same shape).
    - **Real, pre-existing bug surfaced while testing this fix against a from-scratch kind cluster** (first time this migration had ever run against a database `dbt run` had never touched): `d7a67740cfa5`'s `GRANT SELECT ON daily_event_counts TO events_app` assumed that table already existed — it's dbt-owned, not Alembic-owned, so a genuinely fresh database doesn't have it yet, and the migration failed (transactionally rolled back) on that line. Fixed by guarding both the `upgrade()` GRANT and the symmetric `downgrade()` REVOKE with an `IF EXISTS`/`information_schema.tables` check, same idiom the file already used for `CREATE ROLE`'s guard. Not just a workaround: the GRANT was already redundant past its first run anyway, since `dbt/dbt_project.yml`'s marts `+grants`/`+post-hook` re-applies it after every `dbt run` (dbt's `table` materialization drops/recreates the table, wiping grants, each time — see Milestone 7's own bullet below on that).
    - Verified live end-to-end against a fresh `kind create cluster` with no prior state: migration Job completed successfully (owner-role DDL rights confirmed working), app Deployment came up healthy, and `/readyz` (a real `SELECT 1` through the app's actual DB session, not just "pod started") confirmed the app can authenticate and query as `events_app` specifically.
  - App-side wiring done and verified end-to-end against the local dev server: `X-Tenant-ID` header extraction (`app/api/deps.py`'s `TenantIdHeader`, typed `UUID | None` — malformed values rejected by FastAPI's own 422, same as a bad `?limit=abc` already was; only "missing entirely" goes through `EventService`'s `DomainError` -> 400, since that's a business rule, not a type constraint), `EventService.ingest`/`list_events` validation, `PostgresEventRepository.list`'s `.where(EventORM.tenant_id == tenant_id)` filter, and `get_tenant_scoped_session` in `deps.py` (the `SET app.current_tenant` hook via `set_config(..., true)`, wired into `EventRepositoryDep` only — `AnalyticsRepositoryDep` stays on the plain session since `daily_event_counts` has no RLS yet). Tested live: no header -> 400, malformed header -> 422, two different tenants each see only their own events via both `POST /events` and `GET /events`.
    - Hit and fixed one real bug along the way: `Header(alias=..., default=None)` inside `Annotated[...]` raises `AssertionError` at FastAPI route-registration time (startup) — the default has to be a plain `= None` on the parameter, not inside `Header()`. Every parameter using `TenantIdHeader` needs its own `= None`.
  - dbt-side grain change done: `stg_events` casts `tenant_id::uuid` through from the source; `daily_event_counts` grain changed from `(event_type, utc_date)` to `(tenant_id, event_type, utc_date)` (select + group by). `_sources.yml`/both `schema.yml`s updated (new source column, `not_null` tests on `tenant_id` in both models, mart's `unique_combination_of_columns` now includes `tenant_id`). `dbt run && dbt test` both green; verified directly via `psql` that tenant A/B each get their own `daily_event_counts` row instead of one merged cross-tenant count.
  - **Real bug caught and fixed**: dbt's `table` materialization drops and recreates the physical `daily_event_counts` object on every `dbt run` — confirmed directly via `\dp`, this silently wiped the `events_app` grant made in migration `d7a67740cfa5` the very next time `dbt run` executed, which would have made `GET /analytics/daily` fail with a permissions error had it not been caught here. Fix lives in `dbt/dbt_project.yml`'s `marts` config, not another migration: `+grants: {select: ["events_app"]}` (dbt-native, re-applied automatically after every rebuild) and a `+post-hook` that re-enables RLS + recreates the `tenant_isolation` policy (`FOR SELECT` only — `events_app` has no write grant on this table) after every build, for the same reason (policies are attached to the table object and vanish with it).
  - `DailyEventCountORM` (`app/repositories/models.py`) needed `tenant_id` added as part of its composite primary key too — missed in the first pass since it's a separate, dbt-owned read-shape declaration, not something that follows automatically from the dbt model/schema.yml changes.
  - `GET /analytics/daily` now fully tenant-scoped: same shape as `events` — `AnalyticsService.get_daily_counts` rejects a missing tenant with `DomainError` -> 400, `PostgresAnalyticsRepository.get_daily_counts` filters by it, and `get_analytics_repository` in `deps.py` switched from the plain session to `TenantScopedSessionDep` so the `app.current_tenant` hook actually fires for this endpoint too. Verified live: no header -> 400, tenant A/B each get only their own row.
- **Milestone 8 (CDC/streaming)**: done, local docker-compose only (deliberately no kind/k8s deployment — see `PLAN.md`).
  - `tenant_accounts` table (id/name/plan_tier/created_at/updated_at, all server-generated) plus `POST /admin/tenants` / `PATCH /admin/tenants/{tenant_id}` in `app/api/routers/admin.py`. Not tenant-scoped (it's the tenant registry itself, not a tenant-owned resource) and has **no authentication/authorization at all** — consistent with the project having no auth system anywhere by design, but worth naming explicitly since these endpoints are a step more sensitive than the tenant-scoped ones (they can create/modify the tenant registry itself, not just add data under an already-existing tenant). Never acted on since adding real auth is a genuine scope change PLAN.md defers.
  - `docker-compose.yml`: postgres now runs `command: ["postgres", "-c", "wal_level=logical"]` (needs a real container restart, not just `up -d`); added `kafka` (`apache/kafka:4.0.0`, KRaft mode, single-node broker+controller, host port 29092) and `kafka-connect` (`debezium/connect:3.0.0.Final`, port 8083).
  - `docker/register-tenant-accounts-connector.sh`: idempotent, run once per fresh environment after `alembic upgrade head`. Creates the Postgres publication itself rather than letting Debezium do it — `publication.autocreate.mode=filtered` has a known bug in Debezium 3.0.0.Final (`DebeziumException: No table filters found for filtered publication`) — then registers the connector against the existing publication.
  - `streaming/consumer.py` + `streaming/config.py`: standalone consumer process (`uv run --extra streaming python -m streaming.consumer`, own `confluent-kafka` extra so the API image doesn't bundle it), using `confluent_kafka.aio.AIOConsumer`. Logs each captured change (`op`, `before`, `after`), skipping Debezium's post-delete tombstone messages (null value, not a change to react to).
  - **Real bug caught and fixed**: `tenant_accounts.updated_at` used SQLAlchemy's `onupdate=func.now()`, which only fires when SQLAlchemy itself builds the `UPDATE` — confirmed live that a raw `psql UPDATE` left it stale, which a "react to changes from any writer" CDC pipeline can't tolerate. Fixed via migration `95ac45427c05`: a real Postgres trigger (`set_updated_at()` + `trg_tenant_accounts_updated_at`) that fires unconditionally regardless of writer; ORM column switched to `server_onupdate=FetchedValue()`.
  - Verified end-to-end live: connector `RUNNING`, publication exists, `wal_level` confirmed `logical` via `SHOW wal_level`. Ran the consumer, then `INSERT`/`UPDATE`'d `tenant_accounts` directly via `psql` (not the API) — consumer logged both `op=c` (full `after`, `before=None`) and `op=u` (both `before`/`after`, showing the exact diff) within seconds.
- **Milestone 9 (data quality & alerting)**: done.
  - `events.ingested_at` added via migration `80e3a2e19885` — server-generated (`server_default=func.now()`, INSERT-only, never updated), because `occurred_at` is client-supplied/backdatable and can't answer "is the pipeline actually still running." Exposed on the `Event` domain schema and threaded through `stg_events.sql`'s explicit-cast passthrough.
  - `dbt/models/staging/_sources.yml`: `tenant_accounts` added as a second table under the `app` source (previously only `events` was declared). Freshness (`config.freshness`/`config.loaded_at_field` — moved under `config:` as of dbt 1.9/1.10, tripped this up twice before landing correctly) on both: `events` uses `ingested_at`, `warn_after` 1 hour / `error_after` 6 hours (expected near-continuous stream); `tenant_accounts` uses the trigger-maintained `updated_at`, `warn_after` 1 day / `error_after` 7 days (rare admin-driven writes, tight thresholds would just be noise).
  - `GET /health/data-quality` (`app/api/routers/health.py` + new `app/services/data_quality.py`): reads `dbt/target/run_results.json` (Pydantic models `_DbtRunResult`/`_DbtRunResultsFile` validate the file's actual shape rather than trusting raw dict access), returns `503` with the full report as `detail` if any check failed or the file's missing/malformed, `200` with the report otherwise. `app/core/config.py`'s new `dbt_run_results_path` setting is anchored to `Path(__file__).resolve().parents[2]`, not a bare relative string — found live that `fastapi dev` changes its process CWD to the app module's own directory, not the repo root, so a relative path silently 503'd with `FileNotFoundError`.
  - `dbt/tests/assert_events_recent_volume.sql`: first singular test in the project (arbitrary SQL under `dbt/tests/`, not a per-column `data_tests` entry) — fails if `events` has had rows in the last 7 days but zero in the last hour, a row-count/volume signal distinct from freshness's max-timestamp check.
  - **Gap closed** (originally flagged here as not-fixed): `/health/data-quality` now also reads `dbt/target/sources.json` (new `app/core/config.py` setting `dbt_sources_path`, new Pydantic models in `app/services/data_quality.py`) and merges freshness results into the same `checks` list `run_results.json` already populated, so a freshness `WARN`/`ERROR` now surfaces through the endpoint instead of only being visible via a manual `dbt source freshness` run. The merged report's `generated_at` is the *older* of the two files' timestamps, not the newer — deliberately reports the age of the staler piece of dbt evidence, same fail-closed bias as RLS's policy elsewhere in this project.
    - **Correction to this bullet's original claim**: "`assert_events_recent_volume`'s pass/fail surfaces through it correctly" was wrong, confirmed empirically while building this fix. `run_results.json` uses different status vocabularies by node type — models/snapshots report `"success"`/`"error"`/`"skipped"`, but **tests report `"pass"`/`"fail"`/`"warn"`/`"error"`**, never `"success"`. The original `passed=all(check.status == "success" for check in checks)` meant a *passing* test (status `"pass"`) would never satisfy `== "success"` either — so `passed` was almost certainly `False` any time this test appeared in the results at all, regardless of whether it actually passed. Fixed by checking membership in `_PASSING_STATUSES = {"success", "pass"}` instead of a single string equality. `sources.json` freshness results add a third vocabulary (`"pass"`/`"warn"`/`"error"`), covered by the same set.
    - **Second real bug caught along the way**, also pre-existing and unrelated to today's actual change: `app/api/routers/health.py`'s `raise HTTPException(503, detail=report)` passed a raw `DataQualityReport` Pydantic instance as `detail`. FastAPI only runs its JSON encoder on a route's normal `response_model`-typed return value — `HTTPException.detail` goes through Starlette's plain `JSONResponse`, which is just stdlib `json.dumps` and has no idea how to serialize a Pydantic model or a `datetime`. This 503 path had likely never been exercised live before (Milestone 9's own verification checked `dbt`'s CLI output changing, not this endpoint's failure response specifically). Fixed with `report.model_dump(mode="json")`.
    - Verified live, both directions: hit `503` with the full, correctly-serialized report as `detail` while `assert_events_recent_volume` genuinely failed (no recent events) and `events`' source freshness was genuinely `ERROR STALE`; inserted a fresh event via raw `psql`, reran `dbt build`/`dbt source freshness`, and got a clean `200` — confirming both the failure and the success path, not just one.
  - **Real bug caught and fixed**: `PostgresEventRepository.add()` and `PostgresTenantAccountRepository.create()` both called `session.refresh(row)` right after `session.commit()` — already known redundant (SQLAlchemy's `eager_defaults` fetches server-generated values via `RETURNING` on the `INSERT` itself), kept "for explicitness." Turned out to be actively broken for `add()` specifically: `get_tenant_scoped_session` sets `app.current_tenant` via `set_config(..., true)` (`SET LOCAL` semantics), scoped to the transaction that's active at that moment. `commit()` ends that transaction; `refresh()` right after opens a *new* one on the same connection. Confirmed empirically via `psql` that once a custom Postgres GUC has been `SET LOCAL`'d at least once in a session, it reverts to `''` (not `NULL`) once that transaction ends — so the RLS policy's `current_setting(...)::uuid` cast on the refresh's `SELECT` raised `InvalidTextRepresentationError`. Fix: removed both redundant `refresh()` calls (not the load-bearing one in `PostgresTenantAccountRepository.update()`, which genuinely needs it — `onupdate`-generated values aren't eagerly fetched the way `INSERT`'s `server_default` is).
  - Verified live: `dbt source freshness` and `assert_events_recent_volume` both correctly went from passing (right after test inserts) to flagging (`ERROR STALE` / `WARN` / test `FAIL`) once ~2.5 real days had passed with no new writes — three independent mechanisms agreeing on the same true state.
- **Milestone 10 (pipeline correctness, replayability, schema evolution)**: done, local docker-compose only, no new infrastructure — reuses Milestone 8's Kafka/Debezium stack.
  - `tenant_account_changes` table (migration `b520db9ecd67`) — an audit-log projection the consumer upserts into, specifically to prove replay/idempotency rather than just claim it. `source_lsn` is `BigInteger` (caught before ever running: `Mapped[int]` defaults to 32-bit `Integer`, and real Postgres LSNs are 64-bit — would have overflowed on any real workload), unique, used as the dedup key instead of the Kafka offset: LSN identifies the *change* at the source, offset identifies *delivery*, and the two diverge whenever the same change gets redelivered via a different path (connector recreated, topic replayed elsewhere) even though they happen to coincide for a plain consumer-group offset reset.
  - New migration `17aea7bb92f4` grants `events_app` `SELECT, INSERT` on `tenant_account_changes` — every table `events_app` touches needs its own explicit grant, caught via a real `InsufficientPrivilegeError` the first time the consumer tried to write (missed on the first pass).
  - `streaming/consumer.py`'s `handle_change_event` now does `INSERT ... ON CONFLICT (source_lsn) DO NOTHING`, pulling `source_lsn`/`captured_at` from Debezium's `source.lsn`/`source.ts_ms`, `tenant_account_id` from whichever of `after`/`before` isn't null.
  - **Real bug caught and fixed**: the original `AIOConsumer` config never set `enable.auto.commit`/`enable.auto.offset.store` — librdkafka's defaults advance the committed offset the moment a message *batch* is delivered to the app, not when each message is durably processed. Confirmed live: a permission-denied crash on the very first message of a 12-message batch still left the consumer group at zero lag — the whole batch was already marked consumed despite nothing being written, meaning a genuine at-least-once guarantee was silently absent. Fixed: both flags `False`, explicit `consumer.store_offsets(msg)` only after `handle_change_event` succeeds for that specific message.
  - Verified live: reset the consumer group offset to 0 twice. First replay (post-fix) wrote all 10 non-tombstone events fresh (table was empty due to the earlier crash). Second replay reprocessed the identical 10 and logged all as `Duplicate ... skipping` — row count stayed at 10, proving both replay and idempotency, not just one or the other.
  - Schema evolution: `region` added to `tenant_accounts` (migration `58537738edb7`) while the connector and consumer were both live, deliberately without touching `TenantAccountORM` — simulating drift the app's own deploy doesn't control. Confirmed: `after` picked up `region` automatically, no error anywhere in the pipeline, because nothing in this stack (`schemas.enable=false`, unstructured JSONB) validates before/after against a fixed shape. Finding documented directly in the migration's own docstring.
- **Milestone 11 (dbt at scale: incremental, layering, contracts, snapshots)**: done.
  - `daily_event_counts` converted to `materialized='incremental'` (`unique_key=['tenant_id','event_type','utc_date']`, `incremental_strategy='merge'`). Real decision made and documented in the model file: `merge` *replaces* a matched row rather than adding to it, so the `is_incremental()` filter deliberately re-includes the most-recently-loaded day in full (`>=`, not `>`) to correctly recompute that bucket when late events arrive, instead of silently undercounting it. Documented trade-off: only covers lateness within one day's grace period past the last run.
  - New `dbt/models/intermediate/int_events_daily.sql` (new `intermediate:` key in `dbt_project.yml`, `+materialized: view`) — pulls the day-truncation expression out of the mart into its own reusable column.
  - **Real bug caught and fixed along the way**: the deployed `stg_events` Postgres view was missing `ingested_at` entirely — added to `stg_events.sql` back in Milestone 9, but the view itself was never actually rebuilt since. Surfaced because `--select int_events_daily+` only rebuilds *downstream* of a model, not upstream; fixed by rebuilding with `--select stg_events+` instead, which also reinforced when to use `--full-refresh` vs. a plain `dbt run` (definition changed vs. just new data).
  - dbt contract enabled on `daily_event_counts` (`contract={'enforced': true}`, `on_schema_change='fail'` — dbt requires an explicit non-`'ignore'` value for a contracted incremental model). All 4 columns given explicit `data_type`s in `schema.yml`. Proven both directions: a deliberately-wrong `event_count: integer` (Postgres's `count()` actually returns `bigint`) failed the build with dbt's own exact mismatch report; corrected to `bigint`, build succeeded clean.
  - `dbt/snapshots/tenant_accounts_snapshot.sql` — SCD Type 2 via `strategy='timestamp'`, keyed on the trigger-maintained `updated_at` built in Milestone 8 specifically to survive raw-SQL writes. Verified live: mutating `plan_tier` produced a closed old row (`dbt_valid_to` set to the mutation's timestamp) and a new current row (`dbt_valid_from` matching, `dbt_valid_to` null) — full history preserved, both versions queryable.
  - Deliberately not used: dbt's `microbatch` incremental strategy, despite being available (needs `dbt-core>=1.9`, running `1.12`) and purpose-built for exactly the late-arrival problem solved by hand above — documented in the model file as a deliberate choice, since the point of the milestone was deriving the failure mode, not using the feature that automates it away.
- **Milestone 12 (BigQuery as a second dbt target)**: done.
  - New `bigquery` extra in `pyproject.toml` (`dbt-bigquery>=1.12.0`), new `bigquery` target in `dbt/profiles.yml` (`method: oauth` + `impersonate_service_account`, project `project-e8569bd6-524d-42fe-bb9`, dataset `events_analytics`, `location: US`). No downloaded JSON key — the GCP org has `iam.disableServiceAccountKeyCreation` enforced (a Secure-by-Default policy), so key-based auth was never on the table. Auth chain: `gcloud auth login` (personal identity) + `gcloud auth application-default login` (ADC) + `gcloud auth application-default set-quota-project` — impersonation itself is declared explicitly in `profiles.yml`, not hidden in a gcloud login flag.
  - **Real permission issues hit and fixed, twice over**: (1) the IAM Service Account Credentials API wasn't enabled on the project by default — `gcloud services enable iamcredentials.googleapis.com` fixed it. (2) An initial assumption that Project Owner already includes `iam.serviceAccounts.getAccessToken` (so no extra grant would be needed) turned out to be **wrong** — confirmed empirically when `dbt debug --target bigquery` failed with exactly that permission denied. `roles/iam.serviceAccountTokenCreator` had to be explicitly granted on the service account after all; that's precisely why the role exists as distinct from Owner in the first place.
  - This milestone's original plan wording ("point `stg_events`/a mart at BigQuery via `--target bigquery`") turned out to be impossible, confirmed while actually building it: `source()` resolves target-relative, so under `--target bigquery` it'd look for `<bq-project>.public.events`, which doesn't exist — the real event data only ever lives in Postgres, deliberately not replicated. `FUTURE_PLAN.md` updated to reflect this. Built instead: `dbt/models/bigquery/bq_daily_event_counts.sql`, a standalone model under its own `bigquery:` key in `dbt_project.yml` (the `marts:` directory's Postgres-only `+grants`/`+post-hook` would break under the BigQuery adapter), generating its own synthetic data via `UNNEST(GENERATE_ARRAY(1, 2000))` spread across 40 distinct dates via `mod()`, with `partition_by`/`cluster_by` configured. BigQuery dataset `events_analytics` created manually via `bq mk --dataset --location=US` first — dbt doesn't auto-create datasets the way it can rely on an implicit Postgres schema.
  - Partition pruning proven, not just configured: `bq query --dry_run` reported 4200 bytes for an unfiltered scan vs. 111 bytes for a query filtered to one date — roughly 1/40th, matching the 40 partitions exactly.
  - Multi-tenant isolation via `CREATE ROW ACCESS POLICY` compared directly against Postgres RLS (Milestone 7). Confirmed empirically, querying as two distinct principals (toggled via `gcloud config set/unset auth/impersonate_service_account`, since classic `bq` has no per-invocation impersonation flag): the granted service account correctly saw only `tenant-a`; querying as the project Owner (not named in any `GRANT TO`) returned **zero rows**, not everything — the opposite of Postgres RLS's superuser-bypass behavior (`d7a67740cfa5`'s own docstring: "superusers bypass RLS unconditionally"). BigQuery has no owner-exemption for row access policies at all; seeing everything would need the specific `bigquery.rowAccessPolicies.overrideRestriction` permission, which Owner doesn't carry by default. Documented in the model file.
- **Milestone 13 (MongoDB)**: done, local docker-compose only. Closes out `FUTURE_PLAN.md` entirely (Milestones 10–13 all done).
  - New `mongodb` service in `docker-compose.yml` (`mongo:7`, no auth — matches the rest of the compose file's local-dev-only posture), published on host port 27017.
  - New `mongodb` extra in `pyproject.toml`: `pymongo`'s own async API (`AsyncMongoClient`), not Motor — Motor was deprecated 2025-05-14 in favor of it (EOL 2026-05-14), caught before ever landing in the codebase.
  - `streaming/mongo.py`: module-level `AsyncMongoClient` + `event_properties_collection`, same "constructed once at import time, reused for the process's whole lifetime" pattern as `app/core/db.py`'s engine.
  - Second Debezium connector (`docker/register-events-connector.sh`, own publication `dbz_events_publication`/slot `debezium_events`) capturing `public.events` into a new `cdc.public.events` topic — a separate connector/slot from the tenant_accounts one rather than sharing one, per Debezium's own per-connector-slot recommendation.
  - `streaming/consumer.py` now subscribes to both topics and dispatches by table name parsed off `msg.topic()` (a `HANDLERS` dict), not a hardcoded topic string per branch — generalizes if a third table joins later. `consumer_group_id` renamed `tenant-accounts-audit-consumer` → `cdc-audit-consumer` since it now tracks two topics; that's a genuinely new consumer group as far as Kafka's concerned, so the first run reprocessed every past tenant_accounts change from `auto.offset.reset=earliest` (harmless — idempotent on `source_lsn`).
  - Document shape: one document per event, embedded not referenced (`_id=event_id`, `tenant_id`/`event_type`/`occurred_at`/`properties`) — `properties` is 1:1 with its event and always read together with it, no case for a separate referenced collection. `replace_one(..., upsert=True)` keyed on `_id` gives idempotency on replay for free, unlike `tenant_account_changes`'s explicit `source_lsn` unique-constraint approach — a different mechanism because the target shape differs (one current document per event vs. an append-only log).
  - Deliberately no dual-write from the API layer — Postgres stays the only system of record `POST /events` ever touches; Mongo is fed exclusively by CDC reacting to Postgres's own WAL, so it can never diverge from what Postgres actually committed. No `MongoRepository` port in `app/` either, matching that: the app only ever produces (indirectly) the data that ends up in Mongo, it never consumes it back.
  - **Real bugs caught along the way**: two typos in the first draft of the events-projection handler (`event.get("aftet")` instead of `"after"`, and `HALDLERS` instead of `HANDLERS`) — caught by inspection before ever running. Pylance also flagged `after`/`before` as possibly `None` at every subscript site (`after["properties"]` etc.), since narrowing on `op` doesn't type-narrow `after` itself — fixed with explicit `assert after is not None`/`assert before is not None` per branch, which both satisfies the type checker and fails loudly at runtime if a future Debezium event ever violates the assumption.
  - Two transient timing races hit and resolved on their own, confirmed rather than assumed: a `404 No status found for connector` on the very first status check right after registering `events-connector` (the POST returns before Kafka Connect schedules the task — resolved seconds later, confirmed via a repeated `curl`), and the consumer briefly logging `UNKNOWN_TOPIC_OR_PART` for `cdc.public.events` on first subscribe (Kafka only creates a table's topic once Debezium actually produces to it, and subscribe landed a moment earlier — confirmed resolved via `kafka-topics.sh --list` showing the topic once Debezium's snapshot caught up).
  - Verified live, all three handler branches: initial snapshot (`op=r`) projected 4 pre-existing events into Mongo correctly, including one with genuinely empty `properties: {}` — confirmed matching the Postgres source via a direct `psql` query, not just assumed correct because nothing errored. A raw `psql INSERT` with non-trivial nested `properties` (`op=c`) round-tripped into Mongo as a real nested object, not a stringified blob — confirming Debezium/pgoutput does emit JSONB columns as strings under `schemas.enable=false`, and that the handler's `json.loads` correctly reconstructs it. A raw `psql DELETE` (`op=d`) removed the corresponding Mongo document, confirmed via `findOne` returning `null`.
- **docker-compose → kind migration (CDC stack)**: done. Rewrites the whole Milestone 8/10/13 CDC pipeline (Kafka, Kafka Connect, MongoDB, the consumer) to run on the `kind` cluster instead of docker-compose, per the pre-AWS-phase plan. `k8s/base` (Postgres, the app, migrations) stays as-is; the CDC stack points at that *same* in-cluster Postgres rather than a second copy, since the whole point is reacting to changes the already-deployed app is actually making.
  - Two prerequisite fixes landed first, both already covered above in their own entries: the migration-job credential split (Milestone 7 follow-up) and the `daily_event_counts`-ownership guard in `d7a67740cfa5` — both surfaced specifically because this was the first time anything in this project deployed to a genuinely from-scratch kind cluster.
  - New `k8s/overlays/cdc/` Kustomize overlay (`resources: [../../base, ...]`), deliberately separate from `k8s/base` rather than folded in — same "CDC stack is optional, skip it if you're not working on streaming" convention docker-compose already had; a plain `kubectl apply -k k8s/base` still only brings up the app+Postgres.
  - Kafka and MongoDB are `StatefulSet`s with real `volumeClaimTemplates` PVCs (kind's bundled `local-path-provisioner` handles dynamic provisioning), not `emptyDir` like Postgres/the app — deliberate divergence: per-broker persistent volumes is literally the skill `PLAN.md` originally deferred Kafka-on-k8s over, so this migration is specifically where that mechanic needed to actually get exercised, not skipped.
  - The consumer (`streaming/consumer.py`) got containerized for the first time — new `builder-streaming`/`runtime-streaming` stages appended to the existing `Dockerfile` (`docker build --target runtime-streaming`), sharing the same `builder` stage as the app image rather than a separate file, so the shared base-dependency layer gets real Docker build-cache reuse across both images. Both images switched from copying a `.venv` to `UV_PROJECT_ENVIRONMENT=/usr/local` (confirmed empirically against a real `python:3.14-slim` container: pointed at an *existing* interpreter installation, this installs straight into its site-packages with no `pyvenv.cfg`/symlink machinery at all — a venv's isolation is redundant inside a container that's already isolated).
  - Connector registration (creating the two Postgres publications, then registering both connectors with Kafka Connect) needed rethinking for kind — the original `docker/register-*.sh` scripts assume a docker-compose network (`docker compose exec`, `localhost:8083`). New single Job (`cdc-register-connectors`) using an `initContainer` (`postgres:17`, runs the publication SQL) followed by the main container (`curlimages/curl`, registers both connectors) — Kubernetes' own built-in "run to completion before the next container starts" mechanism, rather than two separate Job resources. The SQL/JSON/shell-script content lives as real files under `k8s/overlays/cdc/scripts/`, assembled into a ConfigMap by Kustomize's `configMapGenerator` (content-hash-suffixed, with references auto-rewritten) rather than typed inline into the YAML.
  - **Real bugs hit deploying to an actual from-scratch kind cluster, none of them visible in `kubectl kustomize`'s dry-render**:
    - Postgres crash-looped with `"root" execution of the PostgreSQL server is not permitted`. Root cause: Kubernetes' `command:` field overrides Docker's `ENTRYPOINT`, not `CMD` — unlike docker-compose, where `command:` only overrides `CMD` while `ENTRYPOINT` (postgres's own `docker-entrypoint.sh`, which does the root→`postgres`-user privilege drop) still wraps around it. `postgres-deployment.yaml`'s `command: ["postgres", "-c", "wal_level=logical"]` bypassed that entrypoint entirely. Fixed by switching to `args:` (Kubernetes' equivalent of Docker `CMD`), which is what actually reproduces docker-compose's behavior.
    - Kafka crash-looped on both probes timing out: `command timed out: ... timed out after 1s`. Neither probe had `timeoutSeconds` set, defaulting to Kubernetes' own `1`s — `kafka-broker-api-versions.sh` launches a full JVM to query the broker, which routinely takes longer than 1s to even start, especially under this pod's `250m` CPU request. Fixed with explicit `timeoutSeconds: 5` on both probes.
    - Kafka then crash-looped a second, unrelated way even with the timeout fixed: `java.net.UnknownHostException: kafka-0.kafka` — KRaft needs to resolve its own per-pod hostname (`KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka-0.kafka:9093`) during startup, but Kubernetes only publishes a headless Service's per-pod DNS records for pods that are already `Ready` — a genuine chicken-and-egg deadlock for any self-referential clustered system bootstrapping via a StatefulSet. Fixed with `publishNotReadyAddresses: true` on `kafka-service.yaml`, exactly what Kubernetes' own StatefulSet docs recommend this class of workload set.
  - Verified live end-to-end against the real deployed API (not just raw `psql` this time — the whole stack, app included, was actually up): `POST /admin/tenants` then `POST /events` through a `kubectl port-forward`'d `events-api` Service, watched both flow through Debezium → Kafka → the in-cluster consumer → `tenant_account_changes`/MongoDB, confirmed with a direct `mongosh findOne` matching the posted payload exactly. Along the way, confirmed a real (and non-obvious) behavior on a genuinely fresh database: since the tables started completely empty, Debezium's initial snapshot produced nothing, so the relevant Kafka topics didn't exist until the first real write — and even after that, the consumer took several minutes to notice, not seconds, because `confluent-kafka`'s underlying client only rechecks a previously-unknown topic on its periodic metadata refresh (`topic.metadata.refresh.interval.ms`, defaulting to 5 minutes), never overridden here. This never surfaced during local docker-compose testing, since those tables already had leftover rows from prior sessions.
- **dbt scheduling in kind, and actually fixing `/health/data-quality`**: done. Closes the two remaining gaps the kind migration exposed — `/analytics/daily` erroring (nothing had ever run `dbt build` against the fresh kind Postgres) and `/health/data-quality` being permanently broken in-cluster (it read local files off a filesystem no other pod could ever see).
  - New `k8s/overlays/dbt/` overlay (separate from `overlays/cdc/` — dbt needs nothing from Kafka/Mongo, only Postgres from `k8s/base`), a `CronJob` (`dbt-build`, hourly, matching `events`' own 1-hour freshness threshold, `concurrencyPolicy: Forbid` so overlapping runs can't race the singleton status row below) running a new `Dockerfile` image (`builder-dbt`/`runtime-dbt`, same shared-`builder`-stage pattern as the streaming consumer).
  - New Postgres table `data_quality_runs` (migration `dd949d031fce`) — deliberately a **singleton**, not append-only: `id` constrained to always be `1` via `CHECK (id = 1)` combined with the primary key, so there's structurally never more than one row. `/health/data-quality` only ever needs "what's the state right now," not a growing history of hourly runs with no retention policy.
  - `dbt/scripts/publish_data_quality.py` — after `dbt build`/`dbt source freshness` run, reads the same `target/run_results.json`/`sources.json` this same container just produced, merges them (deliberately duplicating `app/services/data_quality.py`'s merge logic rather than importing it — the `runtime-dbt` image would otherwise need the API's entire dependency tree just to reuse ~30 lines; flagged explicitly in both files' comments so a future change to one doesn't silently drift from the other), and `INSERT ... ON CONFLICT (id) DO UPDATE`s the result. Uses `psycopg2` (not `asyncpg`, unlike the rest of the app) — deliberate, not inconsistent: this is a synchronous, run-once-and-exit script with no concurrent I/O to justify `asyncpg`'s async-only API, and `psycopg2` is already a transitive dependency of `dbt-postgres`, so it needs no new dependency at all. Connects via the same `DBT_*` env vars `profiles.yml` already reads — no separate credential wiring needed.
  - `DataQualityService`/`app/api/routers/health.py` rewritten to query `data_quality_runs` instead of reading local files — this is what actually makes the endpoint work in-cluster, since a Postgres row is trivially readable from any pod, unlike a file that only ever existed in one CronJob's own ephemeral container. New `DataQualityRunORM` in `app/repositories/models.py`, `events_app` granted `SELECT` only (never writes it — only the CronJob does, connecting as the owner role, same as dbt always has).
  - **Three real bugs caught building this, in order**:
    1. `dbt/scripts/run_and_publish.sh`'s first draft had `set -e` in front of `dbt build` — meaning the moment `dbt build` exited non-zero (a *legitimate, expected* outcome; that's the whole point of this system), the script aborted immediately and never reached the publish step at all. Nothing got recorded, the exact opposite of the goal. Fixed by capturing all three steps' exit codes without aborting between them, running every step unconditionally, and only failing the script (so the Job still shows `Failed`, a real signal) after the publish step has already run.
    2. That exposed a second, pre-existing issue: `dbt build` (no `--target`) always tried to build `bq_daily_event_counts` (Milestone 12's BigQuery-only model) against the default Postgres target and failed — every single run, forever, poisoning the CronJob's results with a scoping bug rather than a real data-quality signal. Fixed at the root with dbt's own idiomatic mechanism: `+enabled: "{{ target.name == 'bigquery' }}"` on the `bigquery:` key in `dbt_project.yml`, so that model is skipped entirely under any non-`bigquery` target.
    3. With both of those fixed, a **third, previously-undiscovered bug** surfaced: `daily_event_counts.sql` failed with `relation "public.daily_event_counts" does not exist` on the very first build. Root cause: the `with already_loaded as (select max(utc_date) ... from {{ this }})` CTE sat *outside* the `{% if is_incremental() %}` guard (only the `WHERE` clause using it was actually guarded) — so it compiled into every build unconditionally, including the first-ever one, when `{{ this }}` doesn't exist yet. Postgres has to resolve every table in a query's `FROM` clause to plan it at all, even for a CTE whose result is never consumed downstream, so referencing a nonexistent table there broke the entire query. This model has existed since Milestone 11 and never hit this — every prior build anywhere in this project (local dev, docker-compose) ran against a database where the table already existed from an earlier run; the genuine cold-start path had simply never been exercised until a truly fresh kind Postgres forced it. Fixed by moving the CTE inside the same `is_incremental()` guard as the `WHERE` clause.
  - Verified live: the manually-triggered `dbt-build` Job correctly published a `passed=false` row (real test failure + stale source, confirmed accurate, not a bug) and the Job itself correctly showed `Failed` — both the record and the operational signal working as designed. After the `daily_event_counts` fix, `GET /analytics/daily` returned real aggregated data (`200`, correct count) and `GET /health/data-quality` returned the full merged report as `503`'s `detail` (correctly reflecting the genuine failing state) — both confirmed against the live in-cluster API via `kubectl port-forward`, not just "the Job completed."

- **Pre-AWS local proof: `realtime/` Node/TypeScript SSE relay validated on kind** — done. Builds and proves AWS_PLAN.md Milestone 4's real-time relay service end-to-end on the existing kind cluster before touching EKS, so the AWS phase only has to port a deployment target rather than debug the service itself.
  - `realtime/` (`express` + `kafkajs`, own `package.json`/`tsconfig.json`, built via `tsdown`) — its own independent Kafka consumer group per pod (random `groupId` at startup, deliberately not shared) subscribed to the same `cdc.public.events`/`cdc.public.tenant_accounts` topics `streaming/consumer.py` already reads, fanning out matching CDC changes as Server-Sent Events on `GET /stream/events`, scoped by the same `X-Tenant-ID` header convention as the rest of the app (no auth anywhere in this project, by design — consistent, not a new gap). Purely in-memory, no database of its own — a pod restart loses nothing durable, a reconnecting client just resumes from whatever happens next.
  - `realtime/Dockerfile` — its own file, not appended to the root Python `Dockerfile`: nothing to share (different base image, different package manager) unlike `builder-streaming`/`builder-dbt`'s real cache-sharing reason for reusing `builder`. Three stages (`builder` for the full `npm ci` + `tsdown` build, `deps-prod` for a clean `npm ci --omit=dev`, `runtime` copying only `dist/` + prod `node_modules`), `node:26-alpine` rather than `-slim` — safe here specifically because `express`/`kafkajs` are pure JS with no native bindings, unlike the Python images' `confluent-kafka`/`asyncpg`/`pydantic-core`, where musl-wheel availability is the reason those stay off Alpine.
  - New `k8s/overlays/realtime/` (`resources: [../cdc, ...]`, not `../../base` — needs Kafka, and `cdc`'s own kustomization already chains in `base`) — a 2-replica `Deployment` (deliberately not 1: this is what actually exercises the independent-consumer-group fan-out claim, not just configures it) + `Service`, `KAFKA_BOOTSTRAP_SERVERS=kafka:9092` matching the existing consumer's in-cluster hostname convention, both probes on `/healthz` (the Node service only has the one bare check, no `/readyz`-style dependency check yet).
  - **Real bug caught and fixed**: `kafka.ts` never parsed `events.properties` before broadcasting it — same JSONB-as-a-raw-string behavior `streaming/consumer.py` already discovered and guards against (Milestone 13's own `isinstance(properties, str)` check). Without the equivalent guard, an `events` change would broadcast over SSE with `properties` double-JSON-encoded (a string inside the stringified payload). Fixed with the same guarded-parse idiom, applied to both `before`/`after` right after `JSON.parse`-ing the Debezium message, before dispatching to `onChange`.
  - **Real, reproducible finding, accepted as-is**: both fresh `realtime` pods crash-looped once, identically, within the same millisecond, on first startup — `KafkaJSProtocolError: This server does not host this topic-partition` (`UNKNOWN_TOPIC_OR_PARTITION`) during `kafkajs`'s very first metadata fetch against the single-broker KRaft cluster. Confirmed Kafka itself was healthy and untouched (no restart, no diff) — this looks like a `kafkajs`-specific cold-start quirk on a brand-new client's first connection, not a real outage. Both pods self-healed on Kubernetes' normal restart (`restartPolicy: Always`, not optional config) within about a second. Left unfixed deliberately — the service's whole design already assumes a pod dying loses nothing and a reconnecting client just resumes going forward, so adding an in-process retry loop on top of what Kubernetes already provides for free wasn't worth the complexity for a local proof.
  - **Second real bug found, unrelated to `realtime/` itself**: verification initially produced nothing on either SSE stream after a fresh `POST /events`. Root cause: Kafka Connect's connector registrations had vanished entirely (`GET /connectors` → `[]`). Traced to the *previous* `kafka-connect` container instance crash-looping at boot during the same Docker-Desktop-restart window as everything else (`No resolvable bootstrap urls given in bootstrap.servers` — the identical DNS-not-ready race that hit the Python consumer's pod at the same timestamp); by the time Kafka Connect stabilized, its connector state (which lives in Kafka's own `connect_configs` topic, not local disk) never reappeared. Fixed using the mechanism already built for exactly this: `cdc-register-connectors` is designed to be idempotent and safe to rerun, but a completed `Job` doesn't rerun just because the same manifest is reapplied — had to `kubectl delete job cdc-register-connectors` first, then reapply to recreate it.
  - Verified live: `port-forward`'d directly to each of the 2 `realtime` pods individually (not the `Service` — a `Service` port-forward pins to a single endpoint pod for the whole session, which would never prove two *different* pods both got the broadcast). Two concurrent `curl -N .../stream/events` streams against the same tenant produced **byte-for-byte identical** output, including a live (non-snapshot) `op: "c"` event captured after a real `POST /events` — not just Debezium's initial snapshot replay (`op: "r"`) of pre-existing rows. Confirms the whole pipeline (Postgres → Debezium → Kafka → 2 independent consumer groups → SSE) works end-to-end with zero Redis, exactly as AWS_PLAN.md's own Milestone 4 verification criterion describes — proven here on kind, ahead of doing it again on real EKS replicas behind an ALB.
- **`AWS_PLAN.md` Milestone 0 (AWS foundation)**: done — the first real-AWS milestone, run against a genuinely new AWS account with promotional credit (not the metered 12-month Free Tier). Region: **eu-central-1 (Frankfurt)**, chosen after directly verifying (via AWS's own regional-endpoint docs, not memory) that EKS, RDS, and IAM Identity Center all have full parity there — eu-south-2 (Spain, geographically closest to the operator's Barcelona location) turned out to have the exact same parity once actually checked, but eu-central-1 was kept since it was already mid-setup when that was confirmed. The console's pre-selected `eu-north-1` on account creation was unrelated to physical location — most likely leftover browser/account state, not geo-detection.
  - `terraform/providers.tf`: the real fix needed ternaries on every scalar argument (`access_key`/`secret_key`/all five `skip_*` flags/`s3_use_path_style`), not the `dynamic "endpoints"` mechanism `AWS_PLAN.md`'s own Milestone 0 text implied ("the same way the endpoints block already is") — `dynamic` only generates nested blocks, never scalar arguments, so this was a real error in the plan text, not just an implementation detail. `access_key`/`secret_key` go to `null` (not omitted) under real AWS, which correctly falls through to the standard AWS credential chain (env vars, shared profile, etc.) — decoupling this fix entirely from *how* real credentials get supplied.
  - Credentials: ended up as a **plain IAM user + long-lived access key** (`terraform-events-api`, `AdministratorAccess` attached directly — no group, no permissions boundary, no console access), not IAM Identity Center as initially planned. Enabling Identity Center forces AWS Organizations creation, and this account's own console warned that creating an Organization immediately upgrades the account from its Free Plan to a paid pay-as-you-go plan and **expires the promotional credit balance immediately**. Since Milestone 0 only ever needed "real IAM credentials," not Identity Center specifically, and the whole point of this account is preserving that credit balance under a destroy-after-every-milestone discipline, the short-lived-token approach was abandoned here — deferred to whenever EKS/RDS usage in later milestones forces a real paid-plan upgrade anyway, at which point there's no remaining reason not to revisit it.
  - Real S3 remote state: bucket name changed from the plan's literal `events-api-tfstate` to `events-api-tfstate-<account-id>` (new `data "aws_caller_identity" "current"` in `modules/s3/main.tf`) — S3 bucket names are globally unique across every AWS account on earth, unlike LocalStack, so the original literal name risked colliding with an unrelated bucket somewhere else.
  - `modules/s3/main.tf`'s `force_destroy = true` (a leftover from Milestone 5's LocalStack teardown pass) removed, replaced with `lifecycle { prevent_destroy = true }` — against real infrastructure, a bare `terraform destroy` would otherwise be able to delete the bucket holding the very state it's destroying from. Per-milestone teardown from here on is `terraform destroy -target=module.networking -target=module.iam` (deliberately excluding `module.s3`), with `prevent_destroy` as a hard backstop if that's ever forgotten — same defense-in-depth instinct already applied to multi-tenancy (RLS *and* app-layer filtering, per this repo's own `CLAUDE.md`).
  - New root `terraform/outputs.tf` (`state_bucket_id`) — pure convenience, not functionally required, added only so the account-ID-suffixed bucket name could be copied into `backend.tf` without hand-transcribing a 12-digit account ID.
  - Bootstrap order: applied locally first (creates the real bucket) → `backend.tf`'s `backend "s3"` block rewritten for real AWS (bucket/key/region/`use_lockfile`; no `skip_*`/`endpoints`/test creds) → `terraform init -migrate-state`. Confirmed backend blocks genuinely cannot reference variables at all (not even the `use_locastack`-style ternary pattern `providers.tf` uses) — hardcoded literals are the only option, so unlike `providers.tf`, swapping targets here means editing the file directly. Verified the migration three independent ways: `terraform plan` reported `No changes` immediately afterward, every resource showed its real AWS ID on refresh, and the actual `terraform.tfstate` object was confirmed sitting in the real bucket via `aws s3 ls`.
  - New `terraform/budget.tf`: `aws_budgets_budget`, not a CloudWatch billing alarm — avoids the latter's "Receive Billing Alerts" console opt-in and its us-east-1-only restriction. $10/month, `ACTUAL` cost `GREATER_THAN` a 100% threshold, notifying directly via `subscriber_email_addresses` — no SNS topic needed at all, since (unlike a CloudWatch alarm) AWS Budgets supports direct email notification natively. New `variables.tf` entry `budget_notification_email`, marked `sensitive = true` (redacts the value from `plan`/`apply` CLI output; still stored in cleartext inside the state file itself, acceptable here since the state bucket has public access fully blocked), value supplied only via the gitignored `terraform.tfvars` — never committed, since this repo may end up public as a portfolio piece.
  - New `terraform/terraform.tfvars` (gitignored — added `*.tfvars` to `.gitignore`): `use_locastack = false`, `budget_notification_email = "<real email>"`. `variables.tf`'s `use_locastack` default deliberately left at `true` — since `terraform.tfvars` itself is gitignored, anyone running Terraform here without first creating their own copy (a new machine, a stranger cloning this as a portfolio repo) safely falls back to harmless LocalStack instead of silently attempting real, billable AWS. Same fail-closed instinct as RLS's own unset-GUC behavior, just applied one layer up.
  - `providers.tf`'s hardcoded region changed `us-east-1` → `eu-central-1` — nothing else in this codebase depends on `us-east-1` specifically.
  - Verified live against real AWS throughout, not just planned: `aws sts get-caller-identity` confirmed the IAM user's real ARN; `terraform apply` created all 10 resources (VPC, 2 subnets across different AZs, security group, IAM role/policy/attachment, S3 bucket + versioning + public-access-block) at zero cost (all Free-Tier-eligible or free outright); the budget applied cleanly (`id=938500344309:events-api-monthly-cost`).
- **`AWS_PLAN.md` Milestone 1 (VPC + EKS + ECR): done** — ECR, the VPC/subnet extension, and the `eks` module are all built, applied, and verified live.
  - AWS provider bumped `~> 5.52.0` → `~> 6.62.0` first (verified against HashiCorp's actual v6 upgrade guide via the newly-enabled `terraform` Claude Code plugin/MCP server, not memory) — confirmed zero impact on any resource already applied; the only relevant breaking change found (`aws_eks_addon`'s `resolve_conflicts` split into `resolve_conflicts_on_create`/`resolve_conflicts_on_update`) matters for Milestone 3, not anything built yet. Terraform CLI itself deliberately stayed on `1.15.8`/`required_version = "~> 1.15.8"` — 1.16.0 exists but Homebrew's tap hadn't published it yet, and chasing it wasn't worth the friction.
  - New `terraform/modules/ecr/` — one `aws_ecr_repository` resource with `for_each` over the 4 image names (`app`, `streaming`, `dbt`, `realtime`), not 4 separate blocks. `image_tag_mutability = "MUTABLE"` (not the more hardened `IMMUTABLE`) — deliberate: this project pushes images manually and repeatedly to the same tag until Milestone 10's CI/CD exists to auto-tag by git SHA, and `IMMUTABLE` would reject every repeat push in the meantime. `scan_on_push = true` (free vulnerability scanning) and `lifecycle { prevent_destroy = true }`, same permanent-resource treatment as the S3 state bucket — ECR repos cost near-nothing to leave up, so `module.ecr` joins `module.s3` as permanently excluded from the per-milestone `terraform destroy -target=...` cycle.
  - Verified live, not just applied: `events-api:local` tagged and pushed to `events-api-app`'s real ECR URL, confirmed via a real image digest. Found and explained (not a bug) why the console shows 3 images for that one push: Docker's BuildKit builder wraps every build in an OCI image index (what the `:latest` tag actually points to) plus a small build-attestation manifest, alongside the one real image manifest — standard BuildKit output, not a duplicate push.
  - `modules/networking/main.tf` extended for a real, actually-routable VPC — the prior version (Milestone 5, LocalStack-only) had **no Internet Gateway, no route table, and no route table association at all**; the "public" subnets were never actually internet-routable, just Terraform objects proven against LocalStack. Added: an Internet Gateway + public route table/route/associations (making the existing subnets genuinely public for the first time); 2 new private subnets (CIDR indices 10/11, deliberately non-adjacent to the public subnets' 0/1); a single NAT gateway + EIP (zonal, not the newer "regional" auto-provisioning mode — regional mode auto-expands across AZs based on usage, which works against the deliberate single-NAT cost decision) with an explicit `depends_on` the Internet Gateway per HashiCorp's own documented recommendation (avoids a real creation race); a private route table/route/associations; and `kubernetes.io/role/elb`/`internal-elb` tags on public/private subnets respectively, added now since Milestone 9's ALB Controller needs them and retrofitting later means another apply cycle on a torn-down VPC. Real, accepted trade-off from the single-NAT decision: since only one AZ hosts the NAT gateway, the other AZ's private subnet loses its internet route too if that AZ has an outage — same reasoning as accepting single-NAT in the first place, documented so it isn't rediscovered as a surprise later.
  - `terraform plan` confirmed exactly `13 to add, 2 to change, 0 to destroy` (13 = IGW + 2 route tables + 2 routes + 4 route table associations + 2 private subnets + EIP + NAT gateway; 2 changes = the existing public subnets picking up their new ALB tag in-place) — matches the design's resource count exactly. **Applied for real** (`Apply complete! Resources: 13 added, 2 changed, 0 destroyed`) — the NAT gateway (~$32/mo) is now live and billing; verified via `aws ec2 describe-route-tables` that `events-api-public-rt` routes `0.0.0.0/0` to the IGW and `events-api-private-rt` routes it to `nat-08059d9d2fadd9594`, matching the design exactly.
  - `kubernetes.io/cluster/events-api-eks = shared` added directly into `networking`'s existing subnet `tags` maps (public and private), next to the `role/elb`/`internal-elb` tags already there — not a separate `aws_ec2_tag` resource, which would fight the subnet's own `tags` for ownership and break on a from-scratch rebuild where subnet IDs aren't known at plan time.
  - New `terraform/modules/eks/` — cluster IAM role (trusts `eks.amazonaws.com`, `AmazonEKSClusterPolicy`) + `aws_eks_cluster` with `access_config { authentication_mode = "API" }` (the newer access-entry API, not the old `aws-auth` ConfigMap); an `aws_iam_openid_connect_provider` pointed at the cluster's own OIDC issuer with `client_id_list = ["sts.amazonaws.com"]` (no `thumbprint_list` needed — verified against the real v6 provider docs, EKS's issuer is on a trusted CA path) — this is what Milestone 2's IRSA depends on entirely; node IAM role (trusts `ec2.amazonaws.com`, `AmazonEKSWorkerNodePolicy`/`AmazonEKS_CNI_Policy`/`AmazonEC2ContainerRegistryReadOnly`) + one managed `aws_eks_node_group` in the private subnets, `desired_size = 1`/`max_size = 2`.
  - **Real account-guardrail hit and fixed**: this AWS account is on AWS's newer "Free Plan," which blocks launching any EC2 instance type not flagged `free-tier-eligible` — the node group's default `t3.medium` was rejected outright (`AsgInstanceLaunchFailures: ... not eligible for Free Tier`). Checked the account's actual free-tier-eligible list in `eu-central-1` (`aws ec2 describe-instance-types --filters Name=free-tier-eligible,Values=true`): `t3.micro`/`t4g.micro` (1GB), `t3.small`/`t4g.small` (2GB), `c7i-flex.large` (4GB), `m7i-flex.large` (8GB). Landed on `t4g.small` (Graviton/ARM) — not just cheaper, but the *correct* choice for a different reason found in the same investigation: `docker buildx imagetools inspect` on the already-pushed `events-api-app` image showed `Platform: linux/arm64` (this Mac is Apple Silicon, so the earlier `docker build`/push with no `--platform` flag produced an arm64 image) — an x86_64 node like `m7i-flex.large` could never have run that pod at all. ARM requires `ami_type` set explicitly (`AL2023_ARM_64_STANDARD`) since AWS's undocumented default for an omitted `ami_type` turned out to be `AL2023_x86_64_STANDARD` (confirmed empirically via the replacement `plan`'s diff) — architecture mismatch would have failed the same way a second time.
  - **Second real gap hit and fixed**: `aws_eks_cluster`'s `access_config.bootstrap_cluster_creator_admin_permissions` documents a default of `true` (confirmed against the actual `create-cluster` API docs) but empirically did not create an admin access entry for the applying IAM user — `aws eks list-access-entries` showed only the EKS service-linked role and the node role, nothing for `terraform-events-api`, which is why `kubectl` first failed with "the server has asked for the client to provide credentials." Fixed with an explicit `aws_eks_access_entry` + `aws_eks_access_policy_association` (`AmazonEKSClusterAdminPolicy`, cluster-scoped) keyed off `data.aws_caller_identity.current.arn` rather than a hardcoded ARN — this is the access-entry API doing explicitly what the default was supposed to do implicitly.
  - Verified live, not just applied: `kubectl get nodes` shows the node `Ready` at `10.0.11.55` (confirming it's genuinely in the private subnet, reaching the EKS API through the NAT gateway — the whole reason that had to land first); `kubectl run` with the real ECR `events-api-app:latest` image produced `Successfully pulled image ... in 4.054s` and the container reached `Running`/`Ready: True` — Milestone 1's own verification bar (`AWS_PLAN.md`: "a real EKS cluster is reachable via kubectl; an image pushed to ECR and pulled by a pod") is met end-to-end.
  - **Third real gap hit and fixed**: the AWS web console's EKS "Resources"/"Compute" tabs proxy live calls into the Kubernetes API itself (same as `kubectl`), so they need their own access entry independent of the CLI one — logging into the console showed `Error loading resources / Unauthorized` because the console session runs as the account's **root** user (`terraform-events-api` was deliberately built with no console access at all), which had no access entry either. Added a second `aws_eks_access_entry`/`aws_eks_access_policy_association` pair for `arn:aws:iam::<account-id>:root`, same `AmazonEKSClusterAdminPolicy` scope, ARN built from `data.aws_caller_identity.current.account_id` rather than hardcoded. Accepted as fine here specifically because root is already this account's only console-login identity by design and already holds unbounded account-level power regardless — not a pattern to repeat on a shared/production account.
  - **Torn down at end of session**, per this project's own per-milestone discipline (`AWS_PLAN.md`: "every milestone gets its own `terraform apply`/`terraform destroy` cycle... Nothing stays running between sessions") — `terraform destroy -target=module.eks -target=module.networking` removed exactly the expected 30 resources (17 networking + 13 eks); a follow-up `terraform plan` confirmed a clean mirror (`30 to add, 0 to change, 0 to destroy`) with zero drift on the permanent `ecr`/`s3`/`iam` modules. All three real bugs above are now fixed in the module itself, so the next `apply` should reproduce the whole stack cleanly with no manual intervention — worth treating that as a mini test at the start of the Milestone 2 session. Local `~/.kube/config` still has a now-dead `events-api-eks` context left behind; harmless, but `aws eks update-kubeconfig` will need re-running once the cluster exists again next time.
- **`AWS_PLAN.md` Milestone 2 (RDS + the app, IRSA + RDS IAM auth throughout): fully done and verified end-to-end** — the app itself (not just throwaway test pods) now connects to RDS via IRSA-minted IAM tokens, with zero static passwords involved.
  - New `terraform/modules/rds/` — `aws_db_subnet_group` in the private subnets; a dedicated `aws_security_group.db` with **no inline `ingress`/`egress` blocks** — switched to the current-best-practice `aws_vpc_security_group_ingress_rule`/`egress_rule` resources after checking AWS's own v6 provider docs, which explicitly call the inline-block style legacy. Ingress is a security-group-to-security-group reference (`referenced_security_group_id = module.eks.cluster_security_group_id`, a new eks module output) rather than a CIDR rule — precise regardless of subnet layout. No egress rule at all, deliberately: security groups are stateful (query responses don't need one) and RDS's own AWS-internal integrations (Secrets Manager, backups) don't route through this SG either — left commented out as a one-line addition if a real need (e.g. `postgres_fdw`) ever appears, rather than speculative code kept "just in case." `aws_db_instance` on `db.t4g.micro` (confirmed orderable via `describe-orderable-db-instance-options`; no CPU-architecture constraint here since RDS is fully managed, unlike the EKS node), `manage_master_user_password = true` (password lives only in Secrets Manager, never in `.tfvars`/HCL/state as plaintext), `iam_database_authentication_enabled = true` (turns the *feature* on at the instance level only — doesn't create the IAM-mapped DB user), `publicly_accessible = false`, `skip_final_snapshot = true`.
  - New IRSA pieces added to the existing `modules/iam/` (not a new module — that module's whole purpose is already "the app's AWS identity"; noted but left alone: its pre-existing `aws_iam_role.app` trusting `ec2.amazonaws.com` looks like a dead leftover from before this phase pivoted from raw EC2 to EKS): an `aws_iam_policy` granting exactly `rds-db:connect` scoped to `arn:aws:rds-db:<region>:<account>:dbuser:<rds-resource-id>/events_app` (the RDS-specific ARN format encoding the *Postgres role name*, not an IAM concept — must exactly match `events_app`, the app's existing least-privilege DB role from Milestone 7's RLS work); an `aws_iam_role.app_irsa` whose trust policy only allows `sts:AssumeRoleWithWebIdentity` from `module.eks.oidc_provider_arn` with `StringEquals` conditions on both `:sub = system:serviceaccount:events-api:events-api-app` and `:aud = sts.amazonaws.com` — referencing the OIDC provider dynamically, never the literal issuer ID (it changes on every cluster recreation, confirmed empirically when Milestone 1's mini-rebuild produced a different one).
  - **Sequencing insight, not obvious up front**: IAM auth only maps to a Postgres role that already exists. A fresh RDS instance doesn't have `events_app` until Alembic migrations run (using the master `events` role from Secrets Manager) — so the real order is migrate the schema first (password auth) → one-time `GRANT rds_iam TO events_app` → only then does IRSA/IAM auth work going forward. Both one-off steps had to run as a pod *inside* the cluster, not from a laptop: `aws_security_group.db`'s ingress only allows the EKS cluster's own security group as a source, so RDS is unreachable from anywhere else, deliberately.
  - **Real bugs caught and fixed before/during apply** (same file-check discipline as Milestone 1): the db security group used `name` instead of `name_prefix` (would've produced a literal trailing-hyphen name instead of an auto-suffixed unique one); the `resource_id` output read `.region` instead of `.resource_id` (would have silently built a nonsense `rds-db:connect` ARN); root `main.tf` passed `module.networking.subnet_ids` (public) instead of `private_subnet_ids` for RDS placement; `rds_db_user = "events-app"` (hyphen) instead of `"events_app"` (underscore) — would have scoped the IAM policy to a Postgres role that doesn't exist; `data.aws_region.current.name` is deprecated in the v6 provider in favor of `.region` (caught via a real `terraform plan` deprecation warning, verified against the actual `aws_region` data source docs, not memory).
  - **Automated background security review** (a hook, not something invoked directly this session — trigger mechanism unconfirmed) flagged the just-made commit: (1) the EKS cluster's public API endpoint has no `public_access_cidrs` restriction, open to `0.0.0.0/0` — accepted as-is, since narrowing it means either a home-IP CIDR that breaks on network changes or a private-only endpoint needing a bastion/VPN not built; actual access still requires a valid IAM-signed request either way. (2) Both `terraform-events-api` and `root` hold cluster-admin (`AmazonEKSClusterAdminPolicy`, cluster scope) — already a deliberate Milestone 1 decision (single-operator account, root already holds unbounded account power regardless), left as-is. (3) `aws_db_instance` had no `storage_encrypted` — **fixed** (`storage_encrypted = true`), forcing a destroy/recreate (new `resource_id` — `module.iam`'s `rds_connect` policy auto-updated since it references the value, not a literal; new Secrets Manager secret; RDS endpoint hostname turned out *not* to change on replacement despite expecting otherwise). Migrations + the `GRANT rds_iam` bootstrap were re-run identically against the new instance and verified again (`GRANT ROLE`). A 4th finding was named in the notification but never actually rendered — unidentified.
  - Verified live: migrations ran cleanly end-to-end through `dd949d031fce` via a one-off pod using the real `events-api-app` ECR image; `GRANT rds_iam TO events_app` confirmed via `psql` returning `GRANT ROLE`, against both the original and (after the encryption rebuild) the recreated instance.
  - **Committed**: `9880dd5` — "Milestones 1-2: real AWS VPC/EKS/ECR, then RDS with IRSA-based IAM auth" (19 files; combined rather than split per-milestone, per explicit request after an initial attempt to surgically split the working tree was rejected). `dfd80c1` — the `storage_encrypted = true` fix, committed separately since it landed after the first commit.
  - **The full IRSA/IAM-auth chain is now verified end-to-end, not just built.** `k8s/overlays/aws/service-account.yaml` — a `ServiceAccount` named exactly `events-api-app` in `events-api` (must match the trust policy's `sub` condition exactly), annotated `eks.amazonaws.com/role-arn` with the IRSA role — applied directly (the full overlay `kustomization.yaml` isn't wired up yet, see below). A throwaway pod using this ServiceAccount confirmed EKS's pod-identity webhook actually injects `AWS_ROLE_ARN`/`AWS_WEB_IDENTITY_TOKEN_FILE`, and `aws sts get-caller-identity` showed the assumed IRSA role, not the node's own instance role — the exact silent-failure mode the advisor flagged as worth checking first, and it didn't happen. A second throwaway pod called `aws rds generate-db-auth-token` from inside that same identity and connected via `psql` (`PGPASSWORD` holding the raw SigV4-presigned token, not a URI — the token contains `&`/`=`/`/` characters that would break URI parsing; `sslmode=require` is mandatory for RDS IAM auth) — first attempt hit `FATAL: PAM authentication failed for user "events_app"` (RDS's generic bucket error for any IAM-auth rejection), root-caused by elimination rather than guessed: checked the IAM policy's embedded `resource_id` against the live instance's actual `DbiResourceId` (matched exactly), checked `pg_has_role('events_app','rds_iam','MEMBER')` directly via the master password (`true`) — both correct, so the actual cause was the *first* token simply going stale between generation and use (real wall-clock time passed during an in-between conceptual discussion, eating into the 15-minute window). Regenerating and using a token immediately succeeded: `SELECT current_user` returned `events_app`. Zero password anywhere in that final connection.
  - **App-code side built**: `boto3>=1.43.88` added to `pyproject.toml`'s base dependencies (not an extra — same image runs in both `kind` and `aws`, differentiated only by `APP_ENVIRONMENT`), plus `boto3-stubs[rds]` in the dev group. `app/core/config.py` gained `db_host`/`db_port`/`db_name`/`db_user`/`aws_region` fields, all additive — `database_url` and its default stay exactly as-is for `local`/`kind`. `app/core/db.py` branches on `settings.environment == "aws"` (reusing the existing field rather than adding a redundant flag) and, only in that branch, builds the engine with `pool_pre_ping=True` (the actual safety net — an already-open connection stays valid past token expiry per this file's own note above, so `pre_ping` is what catches an RDS/network-dropped idle connection and transparently reopens it, re-running token-mint in the process; `pool_recycle=600` is just hygiene on top) and a SQLAlchemy `do_connect` event hook registered on `engine.sync_engine` (SQLAlchemy's current documented replacement for the older `creator` callable named in `AWS_PLAN.md`'s original wording — same behavioral guarantee, token minted fresh only at connection-open) that calls a module-scope `boto3.client("rds", ...)`'s `generate_db_auth_token(DBHostname, Port, DBUsername)` — the exact keyword-arg signature was confirmed by actually installing boto3 in an ephemeral `uv run --with boto3` environment and reading `help()`, not assumed. `connect_args={"ssl": "require"}` confirmed via asyncpg's own docs as a real accepted value (not a libpq-only convention). Caught along the way: `boto3-stubs` alone doesn't type individual AWS services — needed the `[rds]` extra for `generate_db_auth_token`'s real signature to show up, confirmed by installing it and checking `type(client)` empirically; and `db_host: str | None` correctly triggered a Pylance error once real RDS types were in place, fixed with a fail-fast `if settings.db_host is None: raise RuntimeError(...)` guard rather than an assertion, narrowing into a local `db_host` used everywhere after (including inside the `do_connect` closure, which doesn't inherit narrowing on `settings.db_host` itself). Image rebuilt and pushed to the same `938500344309.dkr.ecr.eu-central-1.amazonaws.com/events-api-app:latest` tag; verified `boto3.__version__` inside the running container before moving on.
  - **`k8s/overlays/aws/` built and verified via `kubectl kustomize` dry-runs before ever touching the cluster** — caught real bugs this way instead of discovering them mid-apply. Decided (not asked) to use Kustomize's `$patch: delete` for subtracting resources the aws overlay doesn't want (`postgres-deployment.yaml`, `postgres-service.yaml`, `postgres-secret.yaml`, `app-secret.yaml`) layered on `../../base`, plus strategic-merge patches adding/overriding fields (`app-configmap-patch.yaml` for `APP_ENVIRONMENT`/`APP_DB_*`/`APP_AWS_REGION`, `deployment-patch.yaml` and `migration-job-patch.yaml` for the real ECR image + `imagePullPolicy: Always` + a trimmed `envFrom` with the deleted secret's ref removed). Real bugs the dry-run render caught before they could hit the cluster: (1) `migration-job.yaml` also referenced the now-deleted `events-api-secrets` Secret via `envFrom` — missed on the first pass since only `deployment.yaml`'s identical reference had been patched; (2) `postgres-secret.yaml` (`postgres-credentials`) was left orphaned with zero consumers after its Deployment/Service were deleted; (3) a patch file missing `metadata.namespace` fails to match its target at all (`kubectl kustomize` errors `no matches for Id ... [noNs]` rather than silently no-op'ing) — reproduced deliberately in a scratch dir to confirm before trusting the fix; (4) the migration Job's patch fixed `envFrom` but initially missed that it also still had the `kind`-only `image: events-api:local` / `imagePullPolicy: Never`, which would fail with `ErrImageNeverPull` on a real node.
  - **Deliberately left broken, not fixed**: `events-api-migration-secrets`'s `ALEMBIC_DATABASE_URL` still points at `postgres:5432` (the deleted in-cluster Service) rather than the real RDS host. Fixing it now would mean hardcoding the RDS master password from Secrets Manager into a k8s Secret as a throwaway step — the real fix is the migration Job's own IRSA role (still not built; it connects as owner role `events`, which doesn't have `rds_iam` granted, so it can't reuse the app's `events_app`-scoped role) done as one coherent piece of work. Consequence: `events-api-migrate`'s Job pod will fail to connect if run as-is — expected, not blocking, since the schema + `GRANT rds_iam` were already applied manually via one-off pods earlier in this milestone.
  - **Verified live on the real cluster**: namespace was empty before the first apply (confirmed via `kubectl get all -n events-api`, nothing pre-existing to worry about). After `kubectl apply -k k8s/overlays/aws`, the `events-api` Deployment pod came up Ready, and `curl localhost:8000/readyz` (port-forwarded) returned `{"status":"ok"}` — that endpoint runs a real `SELECT 1` through `SessionDep`, so this is the actual application code, under its real ServiceAccount, minting a real IAM token and querying RDS over SSL. Full IRSA chain proven through production code paths, not just test pods.
  - Still open for a future session: the migration Job's own IRSA role + fixing `ALEMBIC_DATABASE_URL` to point at the real RDS host (see above); rebuilding/deploying if any further Alembic revisions get added before that's done, since the Job can't currently run them against RDS.
  - Remember the cost stack while any of this is live: EKS control plane (~$73/mo, no free tier) + the `t4g.small` node + the NAT gateway + RDS (`db.t4g.micro`) all billing simultaneously — same per-milestone `apply`/`destroy` discipline applies before ending a session.
- **`AWS_PLAN.md` Milestone 3 (Strimzi Kafka, MongoDB Community Operator, RDS CDC): fully done and verified end-to-end** — real `POST /events` → RDS logical replication → Debezium → Kafka → the containerized consumer → MongoDB, proven live against real AWS infrastructure, same round-trip bar already proven on kind. EKS/RDS were still live from Milestone 2 at the start of this session (confirmed via `terraform state list` + live `describe-cluster`/`describe-db-instances` calls, not assumed).
  - RDS: `aws_db_parameter_group.logical_replication` (family `postgres18`, confirmed against the live account's actual default engine version rather than assumed) sets `rds.logical_replication=1`, `apply_method="pending-reboot"` — required a real `aws rds reboot-db-instance` afterward, same "static parameter needs a real reboot" lesson as Milestone 2's storage-encryption change. Verified `SHOW wal_level` = `logical` from a one-off in-cluster pod afterward.
  - EBS CSI driver added as an `aws_eks_addon` with its own IRSA role. **Real bug caught before it could cause a silent, hard-to-diagnose failure**: the first draft of the role's OIDC trust policy had only one `condition` block, and its value was wrong — the `:sub` condition (which must match the driver's own ServiceAccount, `system:serviceaccount:kube-system:ebs-csi-controller-sa`) had been overwritten with the `:aud` condition's value (`sts.amazonaws.com`) instead, and the second condition was missing entirely. `terraform plan`/`apply` both succeed against a broken trust policy like this — IAM only validates JSON structure, not semantics — so this would only have surfaced later as an unexplained `AccessDenied` the first time the driver tried to provision a volume. Caught by reading the file back before applying, not by any tool.
  - **Policy version correction, found mid-session**: initially attached the original `AmazonEBSCSIDriverPolicy` managed policy; user asked whether a `V2` existed. It does (`arn:aws:iam::aws:policy/AmazonEBSCSIDriverPolicyV2`, no `service-role/` in the path, published after this model's training cutoff) — compared both policy documents directly: the original has statements with no resource-tag condition at all (`CreateSnapshot`/`ModifyVolume` on any volume, unconditioned) and some `StringLike` wildcard tag matches; V2 scopes every mutating action with `StringEquals` to resources tagged `ebs.csi.aws.com/cluster: "true"` (which the driver already tags its own resources with) and adds a statement blocking the role from stripping its own tracking tags — genuinely tighter least-privilege, switched to it.
  - Node group `desired_size` bumped 1→2 in the same apply as the addon, based on a pre-session capacity estimate. **That estimate was wrong** — confirmed empirically once MongoDB's pod tried to schedule: the Community Operator sets its own default container requests (`mongod` 500m/400M, `mongodb-agent` 500m/400M — neither specified in the CR) totaling 1000m/~800Mi for one pod, needing to land on a single node. By the time Mongo was reached, both nodes were already at 87-98% memory requests from Kafka/Connect/Strimzi/the Mongo operator itself. `FailedScheduling: 1 Insufficient cpu, 2 Insufficient memory` confirmed it directly — required a second `terraform apply` bumping `desired_size`/`max_size` to 3. Lesson documented directly in the implementation plan: an Operator's own default container requests can exceed a hand-estimated budget by a wide margin; verify the actual scheduled pod's `resources` rather than trusting a pre-apply guess.
  - Kafka → Strimzi Operator 1.2.0 (installed via Helm, not Terraform — no Kubernetes/Helm provider anywhere in this repo, same precedent as Milestone 6's raw `helm install`), a single-broker `KafkaNodePool`+`Kafka` CR pair (KRaft, both `controller`/`broker` roles on one node, no Entity Operator — Topic/User CRDs weren't needed and cost a pod this cluster couldn't spare), `gp3` storage. All RF-related config (`offsets.topic.replication.factor`, `transaction.state.log.replication.factor`/`.min.isr`, `default.replication.factor`, `min.insync.replicas`) explicitly set to `1` — a single broker can't satisfy Strimzi's normal multi-broker defaults, same underlying reason the kind migration's raw Kafka container needed `KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1`, just expressed as CR config instead of a Docker env var.
  - **Kafka Connect via Strimzi's own `KafkaConnect`/`KafkaConnector` CRDs, not a hand-rolled Deployment** — corrected mid-scoping from an earlier draft that would have pointed straight at `debezium/connect:3.0.0.Final` (that image's config-injection convention isn't Strimzi's, an unverified compatibility gamble). Built a custom image instead: `FROM debezium/connect:3.0.0.Final AS debezium-plugins` then `FROM quay.io/strimzi/kafka:1.2.0-kafka-4.3.1`, copying `/kafka/connect/debezium-connector-postgres` into Strimzi's own `/opt/kafka/plugins/` (both paths confirmed empirically by actually running each image and listing the directory, not assumed) with `--chown=1001:0` for Strimzi's non-root UID. New 5th ECR repo (`events-api-kafka-connect`). `strimzi.io/use-connector-resources: "true"` annotation + `EnvVarConfigProvider` (`config.providers.env.class`) wired so `KafkaConnector` configs reference `${env:DEBEZIUM_DB_PASSWORD}` — grounded directly against Strimzi 1.2.0's own docs for this exact mechanism — meaning the committed connector manifests never contain the real RDS password.
  - **Real bug: `KafkaConnect` never got a `resources` block**, an inconsistency against the sibling `KafkaNodePool` CR which did get one (both pulled from Strimzi's official example manifests, which don't include `resources` — added it back for the broker but forgot to for Connect). Confirmed the consequence live: `events-connect-connect-0` was evicted for memory pressure (`request is 0, has larger consumption of memory`) — a BestEffort-ish pod with no request is the first thing killed when a node runs low. Self-healed on its own before being noticed, but fixed properly afterward (`250m/512Mi` requests, `500m/1Gi` limits, the same figures the old kind-based hand-rolled Deployment already used).
  - **RDS-side CDC prep**: master user (`events`) already carried `rds_replication` — confirmed via `pg_has_role`, no extra `GRANT` needed (one of two things the implementation plan flagged in advance as "verify empirically, don't assume"). Debezium's credential is a dedicated `debezium-db-credentials` Secret holding the same master password (created imperatively, never committed) — not IRSA, since Debezium has no mechanism to refresh a 15-minute IAM token on a long-lived replication connection. **Real sequencing bug found in the original plan**: it deferred creating the publications to a later task (where a real Job+ConfigMap would exist for reproducibility), but the connectors needed to be registered before that task ran, and rely on `publication.autocreate.mode: filtered` as a fallback — which has the exact Debezium 3.0.0.Final bug this project's kind setup already documented. Fixed by running the (idempotent) publications SQL immediately via a one-off pod, letting the later Job's identical run become a harmless no-op. **Real copy-paste bug in the SQL itself**: the first publication's `IF NOT EXISTS` guard checked for the *second* publication's name instead of its own — worked by coincidence on a fresh run, would have thrown a real error on a partial-retry scenario. Caught by reading the file back before running it.
  - Both `KafkaConnector`s (`tenant-accounts-connector`, `events-connector`) came up `READY` against real RDS. One connector metadata name had a typo (`tenant-accouunts-connector`, double "u") — cosmetic, caught and fixed before applying.
  - MongoDB → Community Operator 0.13.0 (Helm), not the Bitnami chart or DocumentDB — chosen specifically to teach the CRD/Operator lifecycle pattern a second time in a different domain, alongside Strimzi. `members: 1` (not the chart's usual 3) given the capacity budget. **Two real StatefulSet-immutability bugs hit in a row**: (1) the CR's `volumeClaimTemplates` only overrode `data-volume`'s `storageClassName`, missing the operator's second, always-created `logs-volume` template entirely — it had no storage class and no cluster default existed to fall back to (`gp2`/`gp3` both exist, neither marked default), so it could never bind, blocking the pod forever (`no persistent volumes available for this claim and no storage class is set`). Fixing the CR updated fine, but (2) Kubernetes' `StatefulSet.spec.volumeClaimTemplates` is immutable after creation — the operator successfully updated the CR but couldn't propagate the fix to the already-existing StatefulSet object, silently. Required deleting the StatefulSet (safe — zero real data existed yet) so the operator recreated it fresh; hit the *identical* problem a second time on `events-mongo-arb`, an automatically-created, always-present arbiter StatefulSet (kept at `replicas: 0` since arbiters were never configured) that nobody had manually cleaned up the first time. One more real typo caught the same way: `accesModes` (missing an "s") in the PVC template, which would have silently produced a PVC with no access mode set rather than erroring.
  - `k8s/overlays/aws-cdc/` (layers on `../aws`, same chaining pattern `overlays/realtime` uses on `../cdc`) holds the `gp3` `StorageClass`, both `KafkaConnector`s, the `MongoDBCommunity` CR, the consumer Deployment, and the publications Job — `kubectl kustomize` dry-run confirmed it renders cleanly and matches everything already applied ad hoc, before the real `kubectl apply -k`.
  - **Real, substantial gap found only at the very end, during the actual round-trip test**: the containerized consumer (`streaming/consumer.py`) imports `async_session_factory` straight from `app.core.db` — the *main app's* own database module — for its `tenant_account_changes` audit-log writes (Milestone 10). The consumer Deployment never got the `APP_*` env vars that module needs, nor the `events-api-app` ServiceAccount (the one whose IRSA role is trust-scoped to exactly that ServiceAccount name), so on its first real write attempt it crashed trying to connect to the hardcoded local-dev default (`postgresql+asyncpg://events_app:events_app@localhost:5432/events`) — real evidence: `OSError: Multiple exceptions: ... Connect call failed ('127.0.0.1', 5432)`. Fixed by adding both to the Deployment; reuses the exact same ServiceAccount/IRSA role as the main app, correctly, since both connect as the identical `events_app` role for the identical reason.
  - **A genuinely new environment-specific gotcha, confirmed by direct reproduction**: `"$VAR:latest"` inside double quotes silently mangles in zsh — `:l` immediately after a bare `$VAR` is parsed as a history-style lowercase modifier, consuming the colon and the `l` and appending the literal remainder onto the value (`"$ECR_URL:latest"` → `...kafka-connectatest`, confirmed via `zsh -c 'X=foo; echo "$X:latest"'` → `fooatest`). `${VAR}:latest` (braced) sidesteps it entirely. Caused one real failed `docker push` against a mistagged local image before being root-caused and fixed everywhere it appeared.
  - Verified live, the actual bar from `AWS_PLAN.md`'s own Verification section: `POST /events` (after fixing a test-payload miss — `user_id` is required, not optional) produced a real event; the consumer's logs showed `Projected event_id=... op=c into Mongo` for that exact ID; a direct `mongosh` query against `events_projection.event_properties` (not the connection string's default `admin` database — the `readWrite` role is scoped to `events_projection` specifically) confirmed the document, byte-for-byte matching what was posted. The `tenant_accounts` CDC path was proven independently and first, including its own idempotency: a re-delivered change (from `auto.offset.reset=earliest` on the consumer's fresh restart) was correctly logged as `Duplicate ... already recorded, skipping`.
  - Cost stack now live on top of Milestone 2's: 3× `t4g.small` nodes (up from 1), Strimzi's Kafka broker + Kafka Connect + Cluster Operator, MongoDB (1 member) + its Operator, all with `gp3` EBS volumes attached — same per-milestone teardown discipline applies before ending a session.

## Bringing everything back up after a Docker restart

```bash
# 1. Start Docker Desktop itself first (GUI), then:

# 2. docker-compose Postgres (local dev + pytest databases)
cd /Users/vyacheslavbinetsky/Develop/learn/k8s-tf-dbt
docker compose up -d postgres

# 2b. Milestone 8/13's CDC stack + Mongo projection target (only if
# you're actually working on streaming/CDC/Mongo — skip otherwise,
# nothing else depends on these being up)
docker compose up -d kafka kafka-connect mongodb
# Idempotent — re-registers each connector if it's not already there
# (survives container restarts via the named volumes, so this is usually
# a no-op, but costs nothing to rerun):
./docker/register-tenant-accounts-connector.sh
./docker/register-events-connector.sh

# 3. kind cluster — check status before assuming anything's broken
kind get clusters
kubectl cluster-info --context kind-events-api
kubectl -n events-api get pods

# If the node/pods aren't healthy after Docker restarts (kind's node is
# itself a Docker container, and stopped containers don't always resume
# cleanly), the reliable fallback is just rebuilding from what's already
# written — nothing about this cluster is precious:
kind delete cluster --name events-api
kind create cluster --config k8s/kind-config.yaml
# then reapply in order per k8s/README.md (postgres -> wait -> migration job -> wait -> app)

# 4. LocalStack — --persist is no longer optional, see caveat below
lstk start --persist
```

(Note: this `lstk` version, 0.18.0, has no `-d`/detach flag — `lstk start --help` to confirm if a newer version changes this.)

**LocalStack caveat**: LocalStack state does *not* persist across restarts unless started with `--persist`. Right now (post Milestone 5 teardown) there's nothing live to lose — Terraform's state is back to local (currently empty, `backend "s3"` is commented out in `terraform/backend.tf`), so a restart without `--persist` just means the default LocalStack VPC comes back empty, no drift risk.

This becomes higher-stakes again the moment remote state gets re-bootstrapped (uncommenting `backend "s3"` + `terraform init -migrate-state`, e.g. if this project revisits it or the phase-2 real-AWS plan does something similar): once Terraform's own state file lives *inside* a LocalStack-hosted bucket, starting LocalStack without `--persist` wipes the state file itself, not just the resources — worse than the pre-Milestone-5 situation since there'd be no local fallback copy. Use `lstk start --persist` whenever a remote backend is active.

## BigQuery / gcloud auth (Milestone 12)

Not part of the Docker restart flow — BigQuery is a real cloud service, nothing local to bring up. But the auth chain does expire/reset independently of anything in this repo:

- ADC (`gcloud auth application-default login`) tokens can expire or get revoked between sessions. If `uv run dbt debug --target bigquery` (run from `dbt/`) fails with an auth error, redo: `gcloud auth login` + `gcloud auth application-default login` + `gcloud auth application-default set-quota-project project-e8569bd6-524d-42fe-bb9`.
- The `big-query@project-e8569bd6-524d-42fe-bb9.iam.gserviceaccount.com` service account and its `roles/iam.serviceAccountTokenCreator` grant (on `viacheslav.binetskyi.work@gmail.com`, scoped to that one service account) both persist in GCP itself — nothing to redo there across sessions.
- The `events_analytics` BigQuery dataset and `bq_daily_event_counts` table persist too (real cloud storage, not ephemeral). The `tenant_a_only` row access policy on it also persists — still restricts the service account to `tenant-a` only.

## Docker Desktop resource limits

Checked actual usage before suggesting anything — current combined usage across all three (LocalStack, kind, Postgres) is about **1.6GB**, against an 8GB allocation, on a 24GB/12-core Mac. That's not remotely a bottleneck; no change is actually necessary. If you want to be more conservative to leave more headroom for the rest of the system:
- **RAM**: 8GB is already generous (5x actual usage) — fine as-is, or could even drop to 4–6GB if you want more free for other apps, but there's no problem to fix here.
- **CPU**: "unlimited" lets the Docker VM use all 12 cores if something spikes. Nothing we run is CPU-heavy enough to need that, so capping it at something like 6–8 would leave more consistent headroom for the host without affecting anything we're doing — optional, not a fix for an actual problem.
- **Swap (1GB)** and **disk (unlimited)**: both fine, no evidence of pressure on either.
